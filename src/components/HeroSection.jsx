import * as React from "react"


const HeroSection = (props) => {
    return (
        <div className="hero">
            <h1>{props.name}</h1>
            <p>{props.tag}</p>
        </div>
    )
  }
  
  export default IndexPage
  